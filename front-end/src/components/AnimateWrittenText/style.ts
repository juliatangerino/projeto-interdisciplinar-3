import styled from "styled-components";
import convertToRem from "../../utils/convertToRem";

export const ContentText = styled.div`
  width: ${convertToRem(500)};
  margin-right: ${convertToRem(140)};
`;
